apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: starlight
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
