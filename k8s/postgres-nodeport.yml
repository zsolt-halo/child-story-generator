apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: starlight
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30433
  selector:
    app: postgres
