apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: starlight
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path
